import React from 'react';
import Menu from '../Menu';
import Workspace from '../Workspace';

const App = () => {
    return (
        <React.Fragment>
            <Menu/>
            <Workspace/>
        </React.Fragment>
    );
}

export default App;
